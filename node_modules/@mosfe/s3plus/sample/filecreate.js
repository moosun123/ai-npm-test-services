const fs = require('fs');
const path = require('path');
const fn = function (num) {
    function c (key) {
        fs.writeFileSync(path.join(__dirname, `./dist/test${key}.json`), `text${key}`);
    }
    for(let i = 0; i < num; i++) {
        c(i)
    }
}
fn(10)