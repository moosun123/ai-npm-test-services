const Auth = require('../lib/client/DefaultAuth')

let auth = new Auth('node-benchmark-web')

async function test() {
    let res = await auth.getTokenSignature('pigeon-benchmark')
    console.log(res)
}
test().then().catch(err => console.log(err.stack))
