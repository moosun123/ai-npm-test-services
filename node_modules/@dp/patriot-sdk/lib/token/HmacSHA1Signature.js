const crypto = require('crypto')

function signature(token, data) {
    const hmac = crypto.createHmac('sha1', token)
    return hmac.update(data).digest().toString('hex').toUpperCase()
}

module.exports = {
    signature
}
