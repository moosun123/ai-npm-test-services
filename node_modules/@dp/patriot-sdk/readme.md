## usage

```javascript
const Auth = require('@dp/patriot-sdk')

let auth = new Auth(${appname})

auth.getTokenSignature(${name}).then(res)
    console.log(res)
}.catch(err=>{
    //...
})
```


[demo](./test/demo.js)