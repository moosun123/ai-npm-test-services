from setuptools import setup, find_packages

setup(name='cat',
      version='0.1',
      author='yapu.wang',
      packages=find_packages("src"),
      package_dir={'': 'src'},
      package_data={'cat': ["lib/*.so"]},
      )
