//
// Created by Terence on 2018/8/1.
//

#include <gtest/gtest.h>

#include "lib/cat_atomic.h"

TEST(AtomicInt, test) {
    CAT_ATOMICLONG a = 0;

    ASSERT_EQ(1, CAT_ATOMICLONG_INC(&a));
    ASSERT_EQ(0, CAT_ATOMICLONG_DEC(&a));
    ASSERT_EQ(2, CAT_ATOMICLONG_ADD(&a, 2));
}
