const KMS = require('../index')

let res = KMS.getKeyByName('node-benchmark-web', 'test')
res.then(res => console.log('res : ' + res))
    .then(() => {
        return KMS.getKeyByName('node-benchmark-web', 'test', true)
    }).then(res => console.log('res2 : ' + res))
    .catch(err => console.log('err : ' + err))
