var kms = require('../lib/kms')
var os = require('os')

describe('kms', function() {
    switch (os.platform()) {
    case 'darwin': {
        it('should be able to get kms', function() {
            kms.getKeyByName('test', 'test')
        })
        break
    }
    case 'linux': {
        it('should be able to get kms', function() {
            kms.getKeyByName('test', 'test')
        })
        break
    }
    }
})
