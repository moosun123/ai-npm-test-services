import request from './config/axios.config'

export function getProjectId(options: { id: string; appType: string }): Promise<{
  code: number
  message: string
  data?: number
}> {
  return request.post('/safe/auth/project/getProject/byResource', options)
}
