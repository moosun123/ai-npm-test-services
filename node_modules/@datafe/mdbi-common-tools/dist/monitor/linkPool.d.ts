import { Link, TAGS } from './link';
export declare class LinkPool {
    private pool;
    createLink(linkId?: string | number): Link;
    getLink(linkId: string | number): Link | undefined;
    getOrCreateLink(linkId: string | number): Link;
    addPoint(linkId: string | number, pointName: string, tags?: TAGS): void;
    endPoint(linkId: string | number, pointName: string, tags?: TAGS): void;
}
