import Base from './base';
export declare class InfiniteRedirect extends Base {
    private timeThreshold;
    private countThreshold;
    constructor(timeThreshold?: number, countThreshold?: number);
    record(): void;
    private launchInfiniteRedirectFinshedTimer;
    private recordParentUrl;
    private checkCurrentURL;
}
