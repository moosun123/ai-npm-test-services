import { ErrorMessage, ErrorConfig } from './types';
export default class Base {
    metricInst: any;
    owl: any;
    owlVersion: string;
    constructor();
    private getOwlVersion;
    private getMetricInst;
    addError(message: ErrorMessage | string | Error, config?: ErrorConfig): void;
}
