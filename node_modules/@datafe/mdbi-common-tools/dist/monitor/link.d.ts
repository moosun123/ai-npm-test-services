export interface TAGS {
    [tagName: string]: string | number;
}
export declare class Link {
    private pointInfo;
    private isEnd;
    private metricInst;
    id: string | number;
    constructor(id?: string | number);
    private getMetricInst;
    private report;
    add(pointName: string, tags?: TAGS): void;
    end(pointName: string, tags?: TAGS): void;
}
