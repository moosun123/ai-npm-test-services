import { APP_TYPE } from '../auth/constants';
import { GetSafemetaInfoRes } from '../api/safemeta';
declare class SafemetaManager {
    private safemetaInfoMap;
    outbase(options: {
        id: string;
        appType: APP_TYPE;
        message: string;
        mis: string;
        subType?: string;
    }): Promise<boolean | undefined>;
    revokeOutbase(options: {
        id: string;
        appType: APP_TYPE;
        mis: string;
        subType?: string;
    }): Promise<boolean | undefined>;
    getSafemetaInfo(sourceId: string, appType: APP_TYPE): Promise<GetSafemetaInfoRes | undefined>;
}
declare const _default: SafemetaManager;
export default _default;
