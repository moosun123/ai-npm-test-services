import { ApplyStatus, APP_TYPE } from '../constants';
declare class JiangjunlingManager {
    private AuthApplyDynamicContructor;
    private getAuthApplyDynamicContructor;
    applyAuth(options: {
        id: string;
        mis: string;
        appType: APP_TYPE;
    }): Promise<ApplyStatus>;
}
declare const _default: JiangjunlingManager;
export default _default;
