import { AxiosInstance } from "axios";
import { ScConfig } from "./types";
interface SCModel {
    init(): void;
}
declare class SC implements SCModel {
    fetch: AxiosInstance;
    instanceMap: Map<any, SC>;
    constructor(configs: ScConfig);
    init(): void;
    getChildInstance(configs: any): SC;
    setChildInstance(key: any, val: SC): void;
}
export default function singletonWrapper(configs: ScConfig): SC;
export {};
