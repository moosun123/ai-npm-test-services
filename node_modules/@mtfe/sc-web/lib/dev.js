import SCWeb from './index';
const sc = SCWeb({
    componentId: 'sc-demo',
    accessEnv: 'test',
    loginImmediately: false
});
sc.fetch({
    url: '/demo/api/data',
    method: 'GET'
}).then((res) => {
    const nd = document.querySelector('#content');
    if (nd) {
        nd.innerHTML = `
            <p>${res.data.data.join(',')}</p>
            <p>${res.data.message}</p>
        `;
    }
});
//# sourceMappingURL=dev.js.map