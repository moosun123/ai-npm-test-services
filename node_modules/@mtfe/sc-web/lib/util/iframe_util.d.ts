import { ScConfig } from "../types";
declare class LoginIframe {
    private src;
    private env;
    private netEnv;
    private timestamp;
    private onlyLogin;
    private timer;
    constructor(src: string, netEnv: string, timestamp: number, configs?: ScConfig);
    send(): Promise<unknown>;
    private setIframe;
    private getIframe;
    clear(): void;
}
export declare function loginIframe(src: string, netEnv: string, timestamp: number, configs?: ScConfig): LoginIframe;
export {};
