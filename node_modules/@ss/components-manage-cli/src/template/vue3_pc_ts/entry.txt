/* Automatically generated by cm tool */
// import '@buildEntryJS';
importComponentStr
importModuleStr

const components = [
    installComponentStr
];

export function install (app) {
    components.map(component => {
        app.component(component.componentName || component.name, component);
    })
}

export {
    listComponentStr
    listModuleStr
};

export default {
    install,
};
