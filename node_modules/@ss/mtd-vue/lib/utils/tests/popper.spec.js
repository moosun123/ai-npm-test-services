'use strict';

var _testUtils = require('@vue/test-utils');

var _testComponent = require('./popper/testComponent.vue');

var _testComponent2 = _interopRequireDefault(_testComponent);

var _popper = require('../popper');

var _popper2 = _interopRequireDefault(_popper);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var localVue = (0, _testUtils.createLocalVue)();
localVue.mixin(_popper2.default);

describe('popper mixins test', function () {
  it('create', function () {
    var wrapper = (0, _testUtils.mount)(_testComponent2.default, {
      localVue: localVue,
      propsData: {
        showPopper: true
      }
    });
    expect(wrapper.find('.mtd-popper-test').exists()).to.true;
    expect(wrapper.find('.mtd-popper').exists()).to.true;
  });
  it('test option placement', function () {
    var wrapper = (0, _testUtils.mount)(_testComponent2.default, {
      localVue: localVue,
      propsData: {
        showPopper: true,
        placement: 'top'
      }
    });
    expect(wrapper.find('.mtd-popper')).to.exist;
    expect(wrapper.props().placement).to.equal('top');
  });
});