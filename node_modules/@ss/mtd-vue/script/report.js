const { report } = require('@ss/lx-report');

const { name, version } = require('../package.json');

report({
  appName: 'mtd',
  cid: 'c_techportal_3tbp0ifk',
  category: 'data_sdk_techportal',
  value: {
    name,
    version,
  },
});
