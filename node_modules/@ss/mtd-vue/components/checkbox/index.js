import { withCheckboxGroup } from '@components/checkbox-group';
import Checkbox from './checkbox.vue';
import { withFormItem } from '@components/form-item';

const MtdCheckbox = withFormItem(
  'MtdCheckbox',
  withCheckboxGroup('', Checkbox),
);

/* istanbul ignore next */
MtdCheckbox.install = function (Vue) {
  Vue.component(MtdCheckbox.name, MtdCheckbox);
};

export default MtdCheckbox;
