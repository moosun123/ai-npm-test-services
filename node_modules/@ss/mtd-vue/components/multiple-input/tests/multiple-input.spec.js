import { mount } from '@vue/test-utils';
import MultipleInput from '../index';

describe('MultipleInput', function () {
  it('create', () => {
    const wrapper = mount(MultipleInput, {
      propsData: {
        value: []
      }
    });
    expect(wrapper.vm).to.exist;
  });
});
