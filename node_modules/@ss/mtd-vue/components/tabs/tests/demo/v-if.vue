<template>
  <mtd-tabs v-model="activeName1" ref="tabs">
    <mtd-tab-pane label="Tab1" value="Tab1">
      <div v-if="activeName1 === 'Tab1'">示例内容: <br>
      明月几时有？把酒问青天。不知天上宫阙，今夕是何年。
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab2" value="Tab2">
      <div v-if="activeName1 === 'Tab2'">示例内容: <br>
      我欲乘风归去，又恐琼楼玉宇，高处不胜寒。起舞弄清影，何似在人间！
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab3" value="Tab3">
      <div v-if="activeName1 === 'Tab3'">示例内容: <br>
        转朱阁，低绮户，照无眠。 不应有恨，何事长向别时圆？
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab4" value="Tab4" disabled>
      <div v-if="activeName1 === 'Tab4'">示例内容: <br>
        人有悲欢离合，月有阴晴圆缺，此事古难全。但愿人长久，千里共婵娟。
      </div>
    </mtd-tab-pane>
  </mtd-tabs>
</template>
<script>
import Tabs from '@components/tabs';
import TabPane from '@components/tab-pane';

export default {
  components: {
    MtdTabs: Tabs,
    MtdTabPane: TabPane,
  },
  data () {
    return {
      activeName1: '',
    };
  },
  mounted () {
    setTimeout(() => { this.activeName1 = 'Tab1'; }, 300);
  },
  methods: {
    assertion () {
      expect(this.$refs.tabs.showPanel).to.be.true();
      const content = this.$el.querySelector('.mtd-tabs-content');
      expect(content.style.display).to.be.not.eql('none');
    },

    doTest (done) {
      try {
        setTimeout(() => {
          this.assertion();
          done();
        }, 400);
      } catch (e) {
        done(e);
      }
    },
  },
};
</script>
