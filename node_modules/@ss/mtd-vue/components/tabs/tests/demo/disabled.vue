<template>
  <mtd-tabs v-model="activeName1" ref="tabs">
    <mtd-tab-pane label="Tab1" value="Tab1">
      <div>示例内容: <br>
      明月几时有？把酒问青天。不知天上宫阙，今夕是何年。
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab2" value="Tab2">
      <div>示例内容: <br>
      我欲乘风归去，又恐琼楼玉宇，高处不胜寒。起舞弄清影，何似在人间！
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab3" value="Tab3">
      <div>示例内容: <br>
        转朱阁，低绮户，照无眠。 不应有恨，何事长向别时圆？
      </div>
    </mtd-tab-pane>
    <mtd-tab-pane label="Tab4" value="Tab4" disabled>
      <div>示例内容: <br>
        人有悲欢离合，月有阴晴圆缺，此事古难全。但愿人长久，千里共婵娟。
      </div>
    </mtd-tab-pane>
  </mtd-tabs>
</template>
<script>
import Tabs from '@components/tabs';
import TabPane from '@components/tab-pane';
import { triggerClick, waitImmediate } from '@test/util';
export default {
  components: {
    MtdTabs: Tabs,
    MtdTabPane: TabPane,
  },
  data () {
    return {
      activeName1: 'Tab1',
    };
  },
  methods: {
    async doTest (done) {
      await waitImmediate();
      // tab4 is disabled tab
      const tab = this.$el.querySelectorAll('.mtd-tabs-item')[3];
      expect(tab).to.be.not.undefined();
      triggerClick(tab, true, true);
      waitImmediate();
      // not change
      expect(this.activeName1).to.be.eql('Tab1');
      done();
    },
  },
};
</script>
