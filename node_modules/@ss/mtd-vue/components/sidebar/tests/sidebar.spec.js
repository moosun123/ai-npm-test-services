import { mount } from '@vue/test-utils';
import MtdSidebar from '../index';

describe('sidebar', function () {
  it('create', () => {
    const wrapper = mount(MtdSidebar);
    expect(wrapper.vm).to.exist;
  });
});
