import Input from './input.vue';
import { withFormItem } from '@components/form-item';

const MtdInput = withFormItem('MtdInput', Input, {
  withRef: true,
  methods: {
    focus () {
      return this.$refs.wrappedInstance.focus();
    },
    blur () {
      return this.$refs.wrappedInstance.blur();
    },
    select () {
      return this.$refs.wrappedInstance.select();
    },
  },
});

MtdInput.install = function (Vue) {
  Vue.component(MtdInput.name, MtdInput);
};

export default MtdInput;
