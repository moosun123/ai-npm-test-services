import Dropdown from './dropdown.vue';
import createControl from './control';

const MtdDropdown = createControl('MtdDropdown', Dropdown);

/* istanbul ignore next */
MtdDropdown.install = function (Vue) {
  Vue.component(MtdDropdown.name, MtdDropdown);
};

export default MtdDropdown;
