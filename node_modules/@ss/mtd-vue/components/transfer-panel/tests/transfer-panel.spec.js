import { mount } from '@vue/test-utils';
import TransferPanel from '../index';

describe('TransferPanel', function () {
  xit('create', () => {
    const wrapper = mount(TransferPanel);
    expect(wrapper.vm).to.exist;
  });
});
