import TreeNode from './tree-node.vue';
// 此处不能使用 import { makeTreeNode } from '@components/tree' 会造成循环依赖
import createTreeNode from '@components/tree/make';

const MtdTreeNode = createTreeNode('MtdTreeNode', TreeNode);
/* istanbul ignore next */
MtdTreeNode.install = function (Vue) {
  Vue.component(MtdTreeNode.name, MtdTreeNode);
};
export default MtdTreeNode;
