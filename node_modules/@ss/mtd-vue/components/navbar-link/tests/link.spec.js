import {
  mount
} from '@vue/test-utils';
import Link from '../link.vue';

describe('Link', function () {
  it('create', () => {
    const wrapper = mount(Link);
    expect(wrapper.vm).to.exist;
  });
});
