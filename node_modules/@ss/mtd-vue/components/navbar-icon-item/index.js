import NavbarIconItem from '../navbar/icon-item.vue';

/* istanbul ignore next */
NavbarIconItem.install = function (Vue) {
  Vue.component(NavbarIconItem.name, NavbarIconItem);
};

export default NavbarIconItem;
