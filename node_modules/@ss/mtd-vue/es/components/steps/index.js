import Steps from './steps';
import withSteps from './with';

export { withSteps };

/* istanbul ignore next */
Steps.install = function (Vue) {
  Vue.component(Steps.name, Steps);
};

export default Steps;