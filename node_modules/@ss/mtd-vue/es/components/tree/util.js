export function findNearestComponent(element) {
  var target = element;
  while (target && target.tagName !== 'BODY') {
    if (target.__vue__ && (target.__vue__.$options.name === 'TreeNode' || target.__vue__.$options.name === 'MtdTreeNode')) {
      return target.__vue__;
    }
    target = target.parentNode;
  }
  return null;
}

export function contains(source, target) {
  var deep = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;

  var walk = function walk(parent) {
    var children = parent.children || [];
    var result = false;
    for (var i = 0, j = children.length; i < j; i++) {
      var child = children[i];
      if (child === target || deep && walk(child)) {
        result = true;
        break;
      }
    }
    return result;
  };

  return walk(source);
}

export function removeChild(parent, child, fieldNames) {
  // remove node
  var children = parent.children || [];
  parent.children = children;
  var index = children.indexOf(child);
  if (index > -1) {
    children.splice(index, 1);
  }
  // remove data
  var dataChildren = parent.data[fieldNames.children] || [];
  var dataIndex = dataChildren.indexOf(child.data);
  if (dataIndex > -1) {
    dataChildren.splice(dataIndex, 1);
  }
}

export function insertChild(parent, child, index, fieldNames) {
  // insert node;
  var children = parent.children || [];
  parent.children = children;
  if (index === undefined || index < 0) {
    children.push(child);
  } else {
    children.splice(index, 0, child);
  }

  // // insert data
  var childData = child.data;
  if (!parent.data[fieldNames.children]) {
    parent.data[fieldNames.children] = [];
  }
  var dataChildren = parent.data[fieldNames.children];

  if (typeof index === 'undefined' || index < 0) {
    dataChildren.push(childData);
  } else {
    dataChildren.splice(index, 0, childData);
  }
}

export function insertBefore(parent, child, ref, fieldNames) {
  var index = void 0;
  if (ref) {
    index = parent.children.indexOf(ref);
  }
  insertChild(parent, child, index, fieldNames);
}

export function insertAfter(parent, child, ref, fieldNames) {
  var index = void 0;
  if (ref) {
    index = parent.children.indexOf(ref);
    if (index !== -1) index += 1;
  }
  insertChild(parent, child, index, fieldNames);
}