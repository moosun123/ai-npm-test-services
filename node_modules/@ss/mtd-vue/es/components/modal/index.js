import Modal from './modal';
import Inside from './inside';

Modal.COMPONENT = Inside;
/* istanbul ignore next */
Modal.install = function (Vue) {
  Vue.component(Modal.name, Modal);
};

export default Modal;