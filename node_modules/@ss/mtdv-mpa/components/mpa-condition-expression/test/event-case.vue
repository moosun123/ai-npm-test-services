<template>
    <div class="selector-container">
        <mtd-select
            v-model="value.event"
            :disabled="disabled"
            class="event-select"
        >
            <mtd-option
                v-for="item in eventOptions"
                :key="item.value"
                :label="item.label"
                :value="item.value"
            />
        </mtd-select>
        <span class="event-text">的</span>
        <mtd-select v-model="value.type" :disabled="disabled" class="event-select">
            <mtd-option
                v-for="item in typeOptions"
                :key="item.value"
                :label="item.label"
                :value="item.value"
            />
        </mtd-select>
    </div>
</template>
<script lang="ts">
import { Vue, Component, Prop } from 'vue-property-decorator';
@Component
export default class EventCase extends Vue {
    @Prop({
        type: Object,
        default: () => {
            return {
                event: 'view',
                type: 'hours',
            };
        },
    })
    private value;

    @Prop({
        type: Boolean,
        default: false,
    })
    private disabled;

    private eventOptions = [
        {
            label: '浏览页面',
            value: 'view',
        },
        {
            label: '点击页面',
            value: 'click'
        }
    ];

    private typeOptions = [
        {
            label: '总时长',
            value: 'hours'
        },
        {
            label: '总次数',
            value: 'times'
        }
    ];
}
</script>
<style lang="scss" scoped>
.selector-container {
    display: flex;
    align-items: center;
    .event-select {
        width: 110px;
    }
    .event-text {
        margin: 0 10px;
    }
}
</style>
