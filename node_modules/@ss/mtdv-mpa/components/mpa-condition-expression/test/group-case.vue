<template>
    <div class="selector-container">
        <mtd-select
            v-model="type"
            :disabled="disabled"
            class="type-select"
        >
            <mtd-option
                v-for="item in typeList"
                :key="item.value"
                :label="item.label"
                :value="item.value"
            />
        </mtd-select>
        <mtd-select v-model="value.id" :disabled="disabled" class="group-select">
            <mtd-option
                v-for="item in groupList"
                :key="item.id"
                :label="item.name"
                :value="item.id"
            />
        </mtd-select>
    </div>
</template>
<script lang="ts">
import { Vue, Component, Prop } from 'vue-property-decorator';
@Component
export default class GroupCase extends Vue {
    @Prop({
        type: Object,
        default: () => {
            return {
                id: null
            };
        },
    })
    private value;

    @Prop({
        type: Boolean,
        default: false,
    })
    private disabled;

    private type = 0;

    private groupList = [
        {
            id: 1,
            name: '最近活跃用户群',
        },
        {
            id: 2,
            name: '典型人群',
        },
        {
            id: 3,
            name: '示例人群',
        }
    ];

    private typeList = [
        {
            label: '用户属于',
            value: 0
        }
    ];
}
</script>
<style lang="scss" scoped>
.selector-container {
    display: flex;
    align-items: center;
    .type-select {
        width: 110px;
    }
    .group-select {
        margin: 0 10px;
    }
}
</style>
