export const isFunction = (f) => {
    return Object.prototype.toString.call(f) === '[object Function]';
};
export const generateBigCharacter = ( num = 0 ) => {
    return String.fromCharCode(num + 65);
};
