import SCWeb from '@mtfe/sc-web';

export default function (env) {
  return SCWeb({
    projectId: 'auth', // 项目id，必需
    accessEnv: env, // 访问环境，必须，可选值： production,staging,test,development
    componentId: 'auth-apply-dynamic',
    netEnv: 'out',
    version: 1
  });
}
